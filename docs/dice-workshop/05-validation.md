# 验证记录

日期：2026-09-20；Xcode 26.6 (17F113)。

## 核心测试

`Tests/RuleTests.swift` 由 `scripts/test-core.sh` 编译运行，不依赖模拟器或第三方库。已通过 48 项检查：

- 精准/相差计分、零分站点消耗、连击奖励及中断；玩家独立重掷额度。
- 同点对子、保留骰子；风险累计、爆仓和投掷上限自动收手。
- 三模板分别完成 1/2/3/4 人完整对局。
- 重复修订号拒绝、不可达/重复站点拒绝、非法随机源的事务原子性。
- 存档往返、恢复不重抽、试玩隔离、终局去重、损坏主文件备份恢复。
- 分享往返新身份、超大/畸形/未来版本导入拒绝。
- 无法创建目录及进行中磁盘写失败均不发布候选状态。
- 个人最佳持久化，历史淘汰后仍保留。

## UI 测试

`DundaillerealUITests/FlowTests.swift`，实际 XCUITest 操作控件：

1. 正式开局，投骰后终止应用、重启继续并核对点数，完成六轮至结算，检查收藏柜记录。
2. 工坊复制模板、改为单轮单站、保存作品、试玩至结算、返回作品库。

第一轮发现编辑页软键盘影响底部保存操作。修复键盘跟随布局、添加完成输入工具栏后重跑。

iPhone 17 Pro（iOS 26.5）：2 条 UI 流程通过，0 失败。iPhone SE 第 3 代（iOS 26.5）：2 条 UI 流程通过，0 失败。Debug 模拟器构建通过；只有未使用 AppIntents 的系统元数据提取提示，无业务编译警告。测试专用资料保存在独立临时目录；普通启动不会读取测试记录。

## 未验证

iOS 16 运行时、物理 iPhone、第三方分享渠道及真实用户平衡性试玩未完成。模拟器不能证明真机触感或音频体验。

## 最终执行凭据

- 最终核心回归：48 checks passed。
- 最终 Debug 构建与 iPhone 17 Pro UI 回归：2026-09-20 15:54，2 tests，0 failures，TEST SUCCEEDED。
- iPhone SE 第 3 代小屏幕 UI 回归：2026-09-20 15:53，2 tests，0 failures，TEST SUCCEEDED。
- 最终 Xcode 结果包：`/tmp/DiceWorkshopBuild/Logs/Test/Test-Dundaillereal-2026.09.20_15-53-36-+0800.xcresult`（临时目录，清理后可按运行说明重新生成）。
- [普通启动首页截图](assets/implementation-home.png)。普通启动未读取自动测试资料。
- UI 回归后的视觉检查修复了装饰卡片圆角外的底色；已重新通过 Debug 构建并重新安装截图确认。该修复仅增加背景填充，不修改规则和交互。
